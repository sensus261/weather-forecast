// Utilities
import { defineStore } from 'pinia'

export const useAppStore = defineStore('app', {
  state: () => ({
    // TODO: add global states like measure system and other vars passed to api
  }),
})
